import sys
import argparse

parser = argparse.ArgumentParser()

parser.add_argument("Test", action="store")
parser.add_argument(
    "--isTrue", action="store_true", 
)

args = parser.parse_args()

if args.isTrue :
    print(True)
else:
    print(False)

x = args.Test
print(x, type(x))